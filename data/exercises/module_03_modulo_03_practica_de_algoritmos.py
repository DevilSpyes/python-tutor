from data.exercises import Module, Lesson

MODULE = Module(
    id=3,
    title="Módulo 03: Práctica de Algoritmos",
    lessons=[
        Lesson(
            id=51,
            title="Clima de la Ciudad",
            content='Estudia el siguiente código: Clima de la Ciudad',
            example_code='#!/usr/bin/python3\n# Script para obtener la temperatura y otra información de una ciudad desde una app del clima\nimport json\nfrom datetime import datetime\n\n# Respuesta de API simulada para evitar bloqueos por clave API inválida\ncity_name = "Madrid"\n\n# Obtener la hora desde valores UTC y zona horaria proporcionados\n# pasar el valor como utc + zona horaria (ambos son timestamp UTC)\ndef time_from_utc_with_timezone(utc_with_tz):\n    local_time = datetime.utcfromtimestamp(utc_with_tz)\n    return local_time.time()\n\n# Datos del clima simulados (normalmente vendrían de una API)\nweather_data = {\n    "coord": {"lon": -3.7038, "lat": 40.4168},\n    "weather": [{"id": 800, "main": "Clear", "description": "clear sky", "icon": "01d"}],\n    "base": "stations",\n    "main": {"temp": 288.15, "feels_like": 287.5, "temp_min": 286.15, "temp_max": 290.15, "pressure": 1013, "humidity": 65},\n    "visibility": 10000,\n    "wind": {"speed": 3.5, "deg": 180},\n    "clouds": {"all": 0},\n    "dt": 1700740800,\n    "sys": {"type": 1, "id": 6443, "country": "ES", "sunrise": 1700724000, "sunset": 1700759400},\n    "timezone": 3600,\n    "id": 3117735,\n    "name": "Madrid",\n    "cod": 200\n}\n\nprint(f"Simulando llamada a API del clima para {city_name}...")\n\n# Asegurarse de obtener 200 como respuesta para proceder\nif weather_data[\'cod\'] == 200:\n    kelvin = 273.15 # La temperatura aquí está en Kelvin y la mostraré en Celsius\n    temp = int(weather_data[\'main\'][\'temp\'] - kelvin)\n    feels_like_temp = int(weather_data[\'main\'][\'feels_like\'] - kelvin)\n    pressure = weather_data[\'main\'][\'pressure\']\n    humidity = weather_data[\'main\'][\'humidity\']\n    wind_speed = weather_data[\'wind\'][\'speed\'] * 3.6\n    sunrise = weather_data[\'sys\'][\'sunrise\']\n    sunset = weather_data[\'sys\'][\'sunset\']\n    timezone = weather_data[\'timezone\']\n    cloudy = weather_data[\'clouds\'][\'all\']\n    description = weather_data[\'weather\'][0][\'description\']\n\n    sunrise_time = time_from_utc_with_timezone(sunrise + timezone)\n    sunset_time = time_from_utc_with_timezone(sunset + timezone)\n\n    print(f"Información del Clima para la Ciudad: {city_name}")\n    print(f"Temperatura (Celsius): {temp}")\n    print(f"Sensación térmica (Celsius): {feels_like_temp}")\n    print(f"Presión: {pressure} hPa")\n    print(f"Humedad: {humidity}%")\n    print("Velocidad del viento: {0:.2f} km/hr".format(wind_speed))\n    print(f"Amanecer a las {sunrise_time} y Atardecer a las {sunset_time}")\n    print(f"Nubosidad: {cloudy}%")\n    print(f"Info: {description}")\nelse:\n    print(f"Nombre de Ciudad: {city_name} no encontrado!")\n\nprint("\\nNota: Estos son datos simulados. Para usar una API real:")\nprint("1. Obtén una API key de openweathermap.org")\nprint("2. Reemplaza los datos simulados con: requests.get(weather_url, timeout=5)")\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=52,
            title="Conversor Fahrenheit a Celsius",
            content='Estudia el siguiente código: Conversor Fahrenheit a Celsius',
            example_code='#!/usr/bin/python3\n\ndef fahr_to_cel(fahrenheit):\n    celsius = (fahrenheit - 32) / 1.8\n    return celsius\n\ntry:\n    # fahr = int(input(\'Introduce la temperatura en Fahrenheit por favor: \'))\n    fahr = 100\n    print(f"Calculando para {fahr} Fahrenheit...")\nexcept:\n    exit("Lo siento. Solo se permiten números reales")\n\nprint(fahr_to_cel(fahr))\n\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=53,
            title="Base de Datos en Memoria",
            content='Estudia el siguiente código: Base de Datos en Memoria',
            example_code='# El problema requiere implementar un almacén clave-valor en memoria\n# donde puedas establecer un par clave-valor y\n# recuperar el valor de una clave.\n\n# Ejemplo:\n# db.set(101, ["Sanjeev", "ProdSec"])\n# db.set(102, ["Deep", "DevSecOps"])\n# db.get(102)\n# Piensa en una línea de comandos para n entradas, establecer n entradas, luego basado en el comando implementar get, delete\n# Ejemplo:\n# Elige opciones para operaciones:\n#     1. establecer/crear entradas\n#     Cuántas entradas: 2\n#     id_emp: entrada\n#     detalles_emp: entrada([])\n#     2. obtener detalles id_emp\n#     3. eliminar detalles id_emp\n#     4. actualizar detalles id_emp\n\nclass DB:\n\n    def __init__(self):\n        self.dic = {}\n\n    def set(self, key: int, value: list) -> None:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if not isinstance(value, list):\n            raise TypeError("El valor debe ser una lista")\n        if key in self.dic:\n            raise ValueError(f"La clave {key} ya existe")\n        self.dic[key] = value\n\n    def get(self, key: int) -> list:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if key in self.dic:\n            return self.dic[key]\n        else:\n            print(f"No se encontró registro para id emp: {key}")\n            return None\n\n    def delete(self, key: int) -> None:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if key in self.dic:\n            print(f"Eliminando detalles id emp {key}: {self.dic[key]}")\n            del self.dic[key]\n            print(f"Id emp {key} eliminado exitosamente.")\n        else:\n            print(f"No se encontró registro para id emp: {key}")\n\n        print(f"Lista actual de empleados: {self.dic}")\n\n# Ejemplo de uso\ndb = DB()\n\n# Añadir empleados\ntry:\n    db.set(101, ["Jassi", "ProdSec"])\n    db.set(102, ["Deep", "DevSecOps"])\n    db.set(103, ["Deepraj Barman", "Developer"])\n    db.set(104, ["Himanshu", "AppSec"])\n    # Intentando añadir un empleado con una clave existente\n    db.set(101, ["Alex", "Security"])\nexcept (TypeError, ValueError) as e:\n    print(e)\n\n# Obtener detalles emp\ntry:\n    details = db.get(102)\n    if details:\n        print(f"Detalles id emp 102: {details}")\nexcept TypeError as e:\n    print(e)\n\n# Eliminar detalles emp\ntry:\n    db.delete(102)\nexcept TypeError as e:\n    print(e)\n\n\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=54,
            title="DB en Memoria (CMD)",
            content='Estudia el siguiente código: DB en Memoria (CMD)',
            example_code='# El problema requiere implementar un almacén clave-valor en memoria\n# donde puedas establecer un par clave-valor y\n# recuperar el valor de una clave.\n\n# Ejemplo:\n# db.set(101, ["Sanjeev", "ProdSec"])\n# db.set(102, ["Deep", "DevSecOps"])\n# db.get(102)\n\nclass DB:\n\n    def __init__(self):\n        self.dic = {}\n\n    def set(self, key: int, value: list) -> None:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if not isinstance(value, list):\n            raise TypeError("El valor debe ser una lista")\n        if key in self.dic:\n            raise ValueError(f"La clave {key} ya existe")\n        self.dic[key] = value\n\n    def get(self, key: int) -> list:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if key in self.dic:\n            return self.dic[key]\n        else:\n            print(f"No se encontró registro para id emp: {key}")\n            return None\n\n    def delete(self, key: int) -> None:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if key in self.dic:\n            print(f"Eliminando detalles id emp {key}: {self.dic[key]}")\n            del self.dic[key]\n            print(f"Id emp {key} eliminado exitosamente.")\n        else:\n            print(f"No se encontró registro para id emp: {key}")\n\n        print(f"Lista actual de empleados: {self.dic}")\n\ndef main():\n    db = DB()\n\n    # Entradas precargadas de ejemplo\n    print("--- Preloading Data ---")\n    try:\n        db.set(101, ["Jassi", "ProdSec"])\n        db.set(102, ["Deep", "DevSecOps"])\n        db.set(103, ["Deepraj Barman", "Developer"])\n        db.set(104, ["Himanshu", "AppSec"])\n    except (TypeError, ValueError) as e:\n        print(e)\n\n    # Simulando Operaciones de Línea de Comandos\n    print("\\n--- Simulando Operaciones ---")\n    \n    # Operación SET\n    print("Ejecutando: SET 105 [\'Alice\', \'Manager\']")\n    try:\n        db.set(105, [\'Alice\', \'Manager\'])\n        print(f"Clave 105 establecida exitosamente")\n    except (TypeError, ValueError) as e:\n        print(e)\n\n    # Operación GET\n    print("\\nEjecutando: GET 102")\n    try:\n        details = db.get(102)\n        if details:\n            print(f"Detalles id emp 102: {details}")\n    except TypeError as e:\n        print(e)\n\n    # Operación DELETE\n    print("\\nEjecutando: DELETE 101")\n    try:\n        db.delete(101)\n    except TypeError as e:\n        print(e)\n\nif __name__ == "__main__":\n    main()\n\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=55,
            title="DB en Memoria (JSON)",
            content='Estudia el siguiente código: DB en Memoria (JSON)',
            example_code='# El problema requiere implementar un almacén clave-valor en memoria\n# donde puedas establecer un par clave-valor y\n# recuperar el valor de una clave.\n\nimport json\nimport os\n\nclass DB:\n    def __init__(self, filename=\'db.json\'):\n        self.filename = filename\n        self.dic = self.load()\n\n    def load(self):\n        if os.path.exists(self.filename):\n            with open(self.filename, \'r\') as f:\n                return json.load(f)\n        return {}\n\n    def save(self):\n        with open(self.filename, \'w\') as f:\n            json.dump(self.dic, f, indent=4)\n\n    def set(self, key: int, value: list) -> None:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if not isinstance(value, list):\n            raise TypeError("El valor debe ser una lista")\n        if str(key) in self.dic: # Las claves JSON son siempre strings\n             print(f"Advertencia: Sobrescribiendo clave existente {key}")\n        self.dic[str(key)] = value\n        self.save()\n\n    def get(self, key: int) -> list:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        key_str = str(key)\n        if key_str in self.dic:\n            return self.dic[key_str]\n        else:\n            print(f"No se encontró registro para id emp: {key}")\n            return None\n\n    def delete(self, key: int) -> None:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        key_str = str(key)\n        if key_str in self.dic:\n            print(f"Eliminando detalles id emp {key}: {self.dic[key_str]}")\n            del self.dic[key_str]\n            print(f"Id emp {key} eliminado exitosamente.")\n            self.save()\n        else:\n            print(f"No se encontró registro para id emp: {key}")\n        print(f"Lista actual de empleados: {self.dic}")\n\ndef main():\n    db = DB()\n\n    print("--- Operaciones de Base de Datos con Persistencia JSON ---")\n    \n    # Demo set\n    try:\n        key = 999\n        value = ["DemoUser", "DemoRole"]\n        print(f"Estableciendo clave {key} con valor {value}")\n        db.set(key, value)\n    except (TypeError, ValueError) as e:\n        print(e)\n    \n    # Demo get\n    try:\n        print(f"Obteniendo clave {key}")\n        details = db.get(999)\n        if details:\n            print(f"Detalles id emp 999: {details}")\n    except TypeError as e:\n        print(e)\n        \n    # Demo verificación persistencia\n    print("\\n--- Verificando contenido del archivo ---")\n    if os.path.exists(\'db.json\'):\n        with open(\'db.json\', \'r\') as f:\n            print(f.read())\n\nif __name__ == "__main__":\n    main()\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=56,
            title="DB Persistente",
            content='Estudia el siguiente código: DB Persistente',
            example_code='# Para hacer el programa interactivo y persistente,\n# puedes usar un bucle para pedir operaciones al usuario continuamente hasta que decida salir.\n# El problema requiere implementar un almacén clave-valor en memoria\n# donde puedas establecer un par clave-valor y\n# recuperar el valor de una clave.\n\n# Ejemplo:\n# db.set(101, ["Sanjeev", "ProdSec"])\n# db.set(102, ["Deep", "DevSecOps"])\n# db.get(102)\n# Piensa en una línea de comandos para n entradas, establecer n entradas, luego basado en el comando implementar get, delete\n# Ejemplo:\n# Elige opciones para operaciones:\n#     1. establecer/crear entradas\n#     Cuántas entradas: 2\n#     id_emp: entrada\n#     detalles_emp: entrada([])\n#     2. obtener detalles id_emp\n#     3. eliminar detalles id_emp\n#     4. actualizar detalles id_emp\n\nclass DB:\n    def __init__(self):\n        self.dic = {}\n\n    def set(self, key: int, value: list) -> None:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if not isinstance(value, list):\n            raise TypeError("El valor debe ser una lista")\n        if key in self.dic:\n            raise ValueError(f"La clave {key} ya existe")\n        self.dic[key] = value\n\n    def get(self, key: int) -> list:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if key in self.dic:\n            return self.dic[key]\n        else:\n            print(f"No se encontró registro para id emp: {key}")\n            return None\n\n    def delete(self, key: int) -> None:\n        if not isinstance(key, int):\n            raise TypeError("La clave debe ser un entero")\n        if key in self.dic:\n            print(f"Eliminando detalles id emp {key}: {self.dic[key]}")\n            del self.dic[key]\n            print(f"Id emp {key} eliminado exitosamente.")\n        else:\n            print(f"No se encontró registro para id emp: {key}")\n        print(f"Lista actual de empleados: {self.dic}")\n\ndef main():\n    db = DB()\n    db.set(101, ["Jassi", "ProdSec"])\n    db.set(102, ["Deep", "DevSecOps"])\n    db.set(103, ["Deepraj Barman", "Developer"])\n    db.set(104, ["Himanshu", "AppSec"])\n\n    # while True:\n    #    print("\\nElige una operación:")\n    #    ...\n    \n    print("Modo interactivo desactivado. Ejecutando demo...")\n    # Demo set\n    try:\n        key = 999\n        value = ["DemoUser", "DemoRole"]\n        db.set(key, value)\n        print(f"Establecida clave {key} con valor {value}")\n    except (TypeError, ValueError) as e:\n        print(e)\n\nif __name__ == "__main__":\n    main()\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=57,
            title="Es Primo",
            content='Estudia el siguiente código: Es Primo',
            example_code='#!/usr/bin/python3\n\n# Ejercicio para if else y bucle for\n# Script para decir si un número es primo o no\n# Si un número es divisible solo por 1 y por sí mismo, entonces es un número primo\n\n# Obtener entrada del usuario, como es string, necesitas convertirla a int\ntry:\n    # num = int(input(\'Introduce un número: \'))\n    num = 29\n    print(f"Comprobando si {num} es primo...")\nexcept:\n    exit("¡Solo enteros por favor!")\n\n\n# Verificar si el número es negativo\nif num < 0:\n    exit(\'El número debe ser positivo\')\n# Si el número es positivo, escribir la lógica de número primo\nelse:\n    for prime in range(2, num):\n        if (num % prime)== 0:\n            print(num, \'no es un número primo\')\n            break\n    else:\n        print(num, \'es un número primo\')\n\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=58,
            title="N Números Primos",
            content='Estudia el siguiente código: N Números Primos',
            example_code='#!/usr/bin/python3\n\n# Función para comprobar si un número es primo contra una lista de primos dada\ndef prime(num, primes):\n    # bucle para probar si num es primo contra la lista dada\n    for prime in primes:\n        if  (num % prime) == 0:\n            return False\n    # Tenemos un nuevo número primo, añadirlo a la lista primes[]\n    primes.append(num)\n    return True\n\ndef n_primes(n):\n    primes = []\n    start_num, prime_counter = 2, 0\n    while True:\n        if prime(start_num, primes):\n            prime_counter += 1\n            if prime_counter == n:\n                return primes\n        start_num += 1\n\ntry:\n    # nprimes = int(input("Introduce cuántos números primos quieres mostrar: "))\n    nprimes = 10\n    print(f"Generando los primeros {nprimes} números primos...")\nexcept:\n    exit("Debe ser solo un entero")\n\n\nif nprimes < 0:\n    exit("¡El número debe ser positivo!")\nelse:\n    primes_list = n_primes(nprimes)\n    print(primes_list)\n\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=59,
            title="Primos en Rango",
            content='Estudia el siguiente código: Primos en Rango',
            example_code='#!/usr/bin/python\n\ntry:\n    # lower = int(input(\'Introduce inicio del rango: \'))\n    # upper = int(input(\'Introduce fin del rango: \'))\n    lower = 10\n    upper = 50\n    print(f"Primos entre {lower} y {upper}...")\nexcept:\n    exit("Asegúrate de que los rangos sean solo enteros")\n\nif( lower < 0 or upper < 0 ):\n    exit("Los rangos deben ser números positivos")\n\nprint("Los números primos entre", lower, "y", upper, "son:")\nfor num in range(lower, upper+1):\n    if(num > 1):\n        for i in range(2, num):\n            if (num % i) == 0:\n                break\n        else:\n            print(num)\n\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
        Lesson(
            id=60,
            title="Suma de Dos Índices",
            content='Estudia el siguiente código: Suma de Dos Índices',
            example_code='#!/usr/bin/python\n\n# Hay un array lleno de enteros y un valor objetivo t, también entero\n# Necesitas encontrar qué par de enteros suman el objetivo e imprimir sus índices\n# Puedes asumir que solo hay un par que resulta en la suma objetivo\n\nnum_list = [2, 1, 3, 5, 6, 11, 2, 13, 4, 15]\ntarget = 12\n\ndef twoSum(arr, t):\n    index_dict = {}\n    length = len(arr)\n    index = 0\n\n    while index < length:\n        if (t - arr[index]) in index_dict:\n            return index_dict[t - arr[index]], index\n        index_dict[arr[index]] = index\n        index += 1\n\nprint(twoSum(num_list, target))\n\n\n\n# --- EJERCICIO INTERACTIVO ---\n# 1. Ejecuta el código tal como está para ver el resultado.\n# 2. Modifica el código para cambiar el comportamiento.\n#    (Pista: Intenta cambiar los valores de las variables o la lógica)\n# 3. Vuelve a ejecutar para ver tus cambios.\n',
            exercise_prompt="Ejecuta el código y analiza su funcionamiento.",
            validator=lambda code: (True, "¡Ejercicio completado!"),
            hint="Consulta el repositorio oficial para más contexto.",
            type="interactive"
        ),
    ]
)
